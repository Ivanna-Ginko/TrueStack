description: Conflict - article already saved
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 409
        message:
          type: string
          example: Article is already in saved articles
